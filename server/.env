# Flask Configuration
SECRET_KEY='your_very_secret_and_random_key_here' # Generate a strong random key (e.g., using os.urandom(24))
FLASK_ENV='development' # Change to 'production' for deployment

# MongoDB Configuration
MONGO_HOSTNAME='127.0.0.1' # Or your MongoDB server IP/hostname
MONGO_PORT='27017'
MONGO_USERNAME='your_mongo_admin_user' # Optional: If auth is enabled
MONGO_PASSWORD='your_mongo_admin_password' # Optional: If auth is enabled
MONGO_DB_NAME='employee_monitor_db'
MONGO_AUTH_DB='admin' # Database to authenticate against (usually 'admin')

# Agent Configuration (Example - can be more dynamic)
AGENT_API_KEY='super_secret_agent_api_key' # A key the agent uses to authenticate with the API